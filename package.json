{
  "name": "fix-roms-names",
  "version": "1.0.0",
  "description": "web scrapper to fix my rom names, so it can find the correct thumbnail in retroarch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "closest-match": "^1.3.3",
    "got": "^12.1.0",
    "jsdom": "^20.0.0",
    "readline-sync": "^1.4.10",
    "string-similarity": "^4.0.4"
  }
}
